import { theme as ChakraTheme } from "@chakra-ui/react";

const theme = {
  ...ChakraTheme,
  colors: {
    ...ChakraTheme.colors,
    brand: "#E84C4F",
    background: "#F8F5F2",
  },
};

export default theme;
